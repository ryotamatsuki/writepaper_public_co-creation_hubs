# Stage 9 Reproducibility Status
Local-equivalent status is generated/confirmed by `make clean && make all`. Final GitHub Actions status must be appended in the PR report after the PR-head run is inspected. Theory is frozen; journal target is NOT SELECTED.
