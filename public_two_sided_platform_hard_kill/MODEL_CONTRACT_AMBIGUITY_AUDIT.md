# Model Contract Ambiguity Audit

**PASS — no derivation-stopping ambiguity.**

Stage 3's welfare file selects W-A and states that the nationally accessible partner population is partitioned by residence, with symmetric regions receiving their resident share of partner surplus. With two symmetric regions this is one-half of national partner surplus for each region.

Stage 3 did not separately name the symmetric metro access-cost parameter. Stage 4 keeps the generic primitive in the utility and uses the explicitly authorized symmetric notation `kappa_1T=kappa_2T=kappa_T`. This is not a normalization into `p_T`: `kappa_T` is a real access cost whereas `p_T` is private revenue.

No strategic primitive was added or changed.