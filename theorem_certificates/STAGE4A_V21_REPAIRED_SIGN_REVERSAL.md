# Theorem Certificate — Stage4A-v2.1-Repaired-Sign-Reversal

## A. Exact claim identity

Claim ID: `S4A-v21-GLOBAL-WITNESS-SIGN-REVERSAL`

For the existing model at the parameter vector

`beta=.01, gamma=.825, tau=.35`

with the remaining primitives fixed at the project baseline, there exist symmetric public-investment candidates `x_G` and `x_B3` such that:

1. the private pricing continuation in G is solved by an optimizing price `p_G`;
2. `x_G` is an all-regime computationally certified global public best response to `x_G` on `[0,1]`;
3. `x_B3` is an all-regime computationally certified global public best response to `x_B3` when the private price is fixed at the matched `p_G`;
4. local implicit best-response slopes have opposite signs: `BR_G' < 0 < BR_B3'`.

This is an existence/witness result for the baseline functional form. It is not a parameter-wide uniqueness or sign theorem.

## B. Quantifiers

- Existential parameter witness: PASS.
- Public strategy domain: `x_i in [0,1]`.
- Private price is reoptimized after material G deviations.
- Local slope statement: local at the repaired regular equilibrium candidates.
- No claim of uniqueness across all parameter values.
- No claim of necessity or primitive iff characterization.

## C. Assumptions actually used

Economic/model assumptions are the same as the existing two-regional-public-hub plus shared private-platform model. The repaired witness uses only existing primitives.

Material parameter restriction: `kL + tau > v + alpha`, numerically `0.62 > 0.60`. This analytically makes the nonresident rival public route dominated by the outside option at every history.

Baseline functional-form restrictions remain authoritative; no general functional-form theorem is asserted.

## D. Equilibrium / globality certificate

- Feasibility and participation: PASS.
- Public boundaries `0` and `1`: attacked.
- Low-investment/free-riding region: attacked.
- Route ordering / active-set changes: handled through primitive upper-envelope reconstruction.
- Private price continuation after deviations: reoptimized.
- Solver/branch failure semantics: fail closed.
- Global finite-deviation search over `[0,1]`: PASS at computational certification tolerance.
- Candidate vs detected BR differences are numerical-noise scale.

## E. Independent reconstruction

PASS. `stage4a_v21_repaired/code/independent_repaired_audit.py` is a clean-room implementation and does not import the Stage-4 repair solver, the old central-regime solver, or the old exact verifier.

## F. Counterexample search

The audit deliberately targets the old counterexample mechanism and boundary/off-branch histories. The earlier failed canonical vector is preserved as a permanent regression failure. No corresponding profitable deviation is found at the repaired witness.

## G. Generality

Classification: **baseline-only existential computationally certified global witness + local derivative sign result**.

Prohibited wording: generic/global-over-parameters theorem, primitive necessary-and-sufficient condition, universal uniqueness.

## H. Benchmark definition

B3 is the matched-price nested benchmark: the private price is fixed at the equilibrium private price generated by G, so the comparison removes the private repricing response while holding the price level matched. PASS.

## I. Proof/evidence maturity

- Local derivative signs: independently numerically reconstructed and stable across step sizes.
- Global-equilibrium witness: independently computationally certified all-regime; not an analytic full-domain proof.
- Earlier small-beta analytic mechanism remains separate evidence and must not be conflated with the global witness.

## J. Manuscript scope

Maximum defensible claim: the model admits an all-regime computationally certified equilibrium witness at which matched-price B3 public investments are local strategic complements while the full endogenous-pricing game yields local strategic substitutes; a separate local analytic argument identifies the repricing channel behind the reversal.

Stronger prohibited claim: the sign reversal holds globally for all admissible parameters or is characterized by primitive iff conditions.

## K. Final state

**PASS**

Stage-4A verdict: `GO — MATHEMATICAL ADVERSARIAL CERTIFICATION PASS`.
