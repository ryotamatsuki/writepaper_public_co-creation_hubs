# Network Functional-Form Generality

For the audit, write

`b_i=g_i(x_i,n_i^F)`, `b_T=g_T(n_T^F)`

with `C^1` functions and positive relevant derivatives at the canonical equilibrium. This is not a replacement model.

The cutoff/participation/private-price/public-FOC system is continuous in the functions and their first derivatives. Under nonsingular Jacobians/SOCs and the strict Stage-4 inequalities, sufficiently small `C^1` perturbations of the canonical linear functions preserve `M_B3>0>M_G`.

Verdict: **linear network feedback is not locally knife-edge.** No global result is claimed for arbitrary nonlinear network functions.
