# Stage 4M-R Executive Verdict

## Verdict

**NO-GO — STOP THIS THEORY ROUTE.**

Reason: explicit project allocation is economically real and survives the rational-choice/referral gate, but the minimal model yields **global strategic substitution**. Referral creates a genuine fallback matching benefit yet cannot reverse literal project stealing into strategic complementarity on any admissible open set.

The result is stronger than a numerical failure. Exact symbolic and Bernstein-basis certificates establish `Gamma < 0` for all

`0 < l < m < h < a`, `0 < p < 1`, `sigma >= 0`

both with and without referral.

## What survived

- R2 information structure is coherent.
- Endogenous hub choice is literal, rational and payoff-relevant.
- Competition and referral use the same partner-set architecture.
- Partner-side regional surplus is natural incidence and is not needed to manufacture competition.
- A decentralized/planner wedge survives.

## What failed

The required duality does not survive. Referral does not produce an open set with `Gamma > 0`. The old PR #17 diagnostic sign reversal disappears once literal project allocation is introduced.

This triggers the Stage 4M-R kill conditions corresponding to N7/N9: sign reversal would require an unauthorized rescue feature, and the surviving competition result is essentially a project-stealing/submodularity result.

## Routing

**STOP. Do not proceed to Stage 5M-R or Stage 6M-R for this architecture.**
