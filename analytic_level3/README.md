# Analytic Level-3 Upgrade Track

This directory is an experimental analytic-theory track branched from `main` at `6132a4f063012aa34f473b3a9c0185b546486b38`. It does not replace the Stage-8 freeze, Stage-11 manuscript, Stage-12 journal positioning, or the production proof ceiling.

## Target
Determine whether the frozen headline comparison can be upgraded from a certified local existence result to a necessary-and-sufficient analytic characterization inside the regular interior regime:

`BR_i^{B3′}>0>BR_i^{G′}`.

The hard-kill standard requires primitive or economically transparent primitive-derived conditions. A merely formal inequality in endogenous equilibrium roots is classified as Level-3C and is not sufficient for a full Level-3 GO.

## Preservation rule
No file under `paper/`, `sections/`, `generated/`, the Stage-8 freeze, Stage-11 gate, or Stage-12 positioning is modified by this track. Existing numerical outputs remain the reconciliation authority.

## Central warning
The headline compares two distinct public equilibria. `G` uses `(x^G,p^G)`; `B3` fixes `p=p^G` but re-solves the public game at `x^{B3}`. Consequently an exact price-correction decomposition evaluated at one state is a mechanism identity, not by itself a primitive necessary-and-sufficient condition for the headline comparison.