# Benchmark Register

## B3 — Two public hubs + private hub, fixed private price
Retain `H_1,H_2,H_T` and two-sided participation; fix `p_T=bar p`. For the headline controlled comparison, `bar p` equals the G-equilibrium fee `0.0227467` at the frozen witness. B3 identifies the fixed-price public-public network channel; it is not a recommended policy regime.

## G — Full game
`G_1,G_2` choose investments first; the private operator chooses `p_T^*(x_1,x_2)`; participation is endogenous. G adds the optimized price-response contribution to the public-public reduced strategic derivative.

## Other Stage-4 benchmarks
B1 (one public + private), B2 (two public/no private), B4 (`beta=0`) and coordinated-public objects remain supporting nesting/essentiality diagnostics only. They may not be promoted to headline contributions without reopening earlier gates.