# Welfare Register

Regional project surplus in the central regime:
`PS_i=∫_s^{t_i}(zq_T-kappa_T-p_T)dz+∫_{t_i}^1(zq_i-kappa_L)dz`.

With partner value `b_h=n_h^P`, national partner surplus on platform `h` is `b_h^2/2`; symmetric residence ownership assigns `b_h^2/4` to each region.

`W_i=PS_i+(b_1^2+b_2^2+b_T^2)/4-gamma x_i^2/2`.
`Pi_T=p_T n_T^F`.
`W^N=W_1+W_2+Pi_T` under zero private real operating cost.

Exact result: private fee payments are regional outflows but cancel against private profit at aggregate level.

Witness values:
- G: `W_i≈.223019`, `Pi_T≈.012603`, `W^N≈.458642`.
- B3: `W_i≈.210910`, `Pi_T≈.013891`, `W^N≈.435711`.
- G−B3: `W^N≈+.022931`.

The ranking is a local numerical witness only. There is no frozen claim that strategic substitutes are welfare-improving or that G globally dominates B3.