# Proposition Register

## P2-R4 — sole primary contribution
There exists a regular interior local parameter region containing the frozen witness in which
`BR_i^{B3′}>0>BR_i^{G′}`.

Canonical language: two decentralized regional public investments can be strategic complements when the shared private hub's fee is fixed yet strategic substitutes when the private hub optimally reprices.

Official Stage-8 status: **NUMERICALLY SUPPORTED ONLY**, with fine-grained status **NUMERICALLY CERTIFIED EXISTENCE + LOCAL CONTINUITY RESULT**. This category is deliberately conservative: it is not a global closed-form sign theorem.

## Supporting objects
- P2-R1 fixed-price complementarity: benchmark/component overlap only.
- P2-R2 `p^*_{T,x_i}<0`: known/structurally close component; at witness `-0.016505`.
- P2-R3 negative endogenous-price contribution: supporting mechanism, not independent novelty.
- P2-R5 `beta=0` removes relevant B3 complementarity: supporting essentiality.

P1-R and P3–P5 remain rejected as headline contributions.