# Hotelling / Characteristic-Space Isomorphism Hard Kill

This is the decisive Stage 4 test.

For endpoint characteristics `z_1=0`, `z_2=1`, define

`Q_i := x_i(c+u)`,
`t_i := x_i u`,
`lambda := u/(c+u)`.

Then `t_i=lambda Q_i` and, exactly,

`U_1(theta)=Q_1-t_1|theta-z_1|-kappa`,

`U_2(theta)=Q_2-t_2|theta-z_2|-kappa`,

`U_T(theta)=m-p_T-kappa`.

The public investment cost becomes

`gamma Q_i^2/[2(c+u)^2]`.

The local partner-incidence term is a quality-weighted host benefit

`2 integral_{D_i}(Q_i-t_i distance_i(theta)) dtheta`.

No probability distribution over realized partners, no order statistic, no correlation of partner draws, no failed-match state, and no search sequence remains after taking risk-neutral expectations. Every Stage 4 equilibrium expression is generated from the deterministic characteristic payoff above.

An informed spatial-IO referee can recover the provider partitions, flat metro demand interval, midpoint private best response, and public strategic derivatives from this transformed game without using matching theory.

The fact that `theta` is called a technological need rather than a location/taste does not alter the game. Label stripping therefore succeeds.

**Verdict: FAIL — EXACT CHARACTERISTIC-SPACE ISOMORPHISM.**

This triggers `NO-GO — MATCHING STRUCTURE COLLAPSES` under the frozen Stage 4 decision tree.