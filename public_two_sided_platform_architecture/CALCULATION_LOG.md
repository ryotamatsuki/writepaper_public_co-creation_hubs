# Calculation Log

Stage 3 used only reduced-form/symbolic prechecks; it did not solve the full game.

Verified identities used for the selected interior architecture:

- partner participation: `n_i^P=rho+x_i+beta n_i^F`, `n_T^P=rho_T+beta n_T^F`;
- outside project cutoff: `z_h0=(kappa_rh+p_h)/(v+alpha n_h^P)`;
- pairwise project cutoff: `z_hk=[(kappa_rh+p_h)-(kappa_rk+p_k)]/[alpha(n_h^P-n_k^P)]` where defined;
- private interior FOC: `D_T+p_T D_{T,p}=0`;
- private response IFT: `p^*_{x_i}=-(D_{T,x_i}+p_T D_{T,px_i})/(2D_{T,p}+p_T D_{T,pp})`.

No sign reversal or global equilibrium claim is made at Stage 3.