# Private Platform Essentiality Test

B2 removes `H_T`; B3 keeps `H_T` but fixes its price.

P2-R requires the terms generated by `p_T^*(x_1,x_2)`. If B2/B3 already reproduce the same qualitative theorem, P2-R fails.

Stage 3 verdict: private strategic pricing is structurally essential and the selected model gives `p_T` a one-dimensional interior optimization problem. The theorem-level essentiality remains a Stage 4 gate.