# Second Public Platform Essentiality Test

Removing `H_2` leaves B1 and the known one-public/private price response. It eliminates any public-public reaction slope and therefore the P2-R object by definition.

This establishes structural—not theorem-level—essentiality of the second public platform.

Stage 4 must still show that the second public platform is not merely a replicated demand block: the effect of `x_1` on `p_T^*` must alter the marginal incentive for `x_2` qualitatively relative to B3.