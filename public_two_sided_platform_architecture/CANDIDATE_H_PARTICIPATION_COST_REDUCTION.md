# Candidate H — Redundancy Diagnosis and Replacement

The originally required participation-cost-reduction candidate is algebraically equivalent to Candidate G whenever `x_i` enters utility additively: raising utility by `x_i` and lowering real participation cost by `x_i` generate the same reduced form.

Under the redundancy rule it is replaced by **H' — Cross-Side Conversion-Efficiency Investment**:

`n_i^P = rho + beta(x_i)n_i^F`, with `beta'(x_i)>0`.

H' makes public investment improve the effectiveness with which projects attract partners rather than shifting utility directly.

Risk: it mechanically writes the cross-side channel into the strategic control and complicates the B4 restriction.

Score: **306/375**. Rejected under K1 risk.